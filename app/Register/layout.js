export const metadata = {
    title: 'Register | EdHotel',
    description: 'Create your EdHotel account and start booking.',
};

export default function RegisterLayout({ children }) {
    return <>{children}</>;
}
